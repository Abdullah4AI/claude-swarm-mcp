/**
 * claude-swarm-mcp
 *
 * Peer discovery, messaging, task delegation, and shared snippets
 * for Claude Code instances working as a swarm.
 *
 * This package has three entry points:
 *   - server.ts  — MCP server (spawned by Claude Code, one per instance)
 *   - broker.ts  — Shared broker daemon (auto-launched, one per machine)
 *   - cli.ts     — CLI for managing the swarm
 *
 * See README.md for setup and usage.
 */

export {} // module marker
